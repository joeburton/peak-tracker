// Types are inferred from Zod schemas — see src/lib/validation/schemas.ts
export type {
  PeakList,
  Peak,
  UserProgress,
  PeakListStatistics,
  RegionalStatistics,
} from '@/lib/validation/schemas'
